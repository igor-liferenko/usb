ISR(...)
{
  if (XXXE) { /* can it trigger? (not necessary to check this if it is enabled and never disabled; if it is the only one that may be enabled in this ISR, this check is not necessary) */
    if (XXXI) { /* did it trigger? (not necessary if it is the only one for which XXXE is set in
                   this ISR) */
      ...
    }
  }
  if (YYYE) { ...
}

